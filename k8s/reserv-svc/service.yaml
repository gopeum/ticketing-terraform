apiVersion: v1
kind: Service
metadata:
  name: reserv-svc
  namespace: ticketing
spec:
  selector:
    app: reserv-svc
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
